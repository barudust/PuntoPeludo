<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.PuntoPeludo" parent="Theme.Material3.DayNight.NoActionBar">

        <!-- Colores primarios con la nueva paleta -->
        <item name="colorPrimary">@color/azul_medio</item>
        <item name="colorPrimaryVariant">@color/azul_claro</item>
        <item name="colorOnPrimary">@color/white</item>

        <!-- Colores secundarios con la nueva paleta -->
        <item name="colorSecondary">@color/azul_claro</item>
        <item name="colorSecondaryVariant">@color/azul_oscuro</item>
        <item name="colorOnSecondary">@color/black</item>

        <!-- Color de fondo de la aplicación -->
        <item name="android:colorBackground">@color/azul_claro</item>

        <!-- Customize your light theme here. -->
    </style>

    <style name="Theme.PuntoPeludo" parent="Base.Theme.PuntoPeludo" />
</resources>
